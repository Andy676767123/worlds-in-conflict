# Worlds in Conflict

A browser-based strategy world simulation built with React, TypeScript, and Vite.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The Vercel settings are: Framework `Vite`, build command `npm run build`, output directory `dist`.
